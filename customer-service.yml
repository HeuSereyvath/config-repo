server:
  port: 6666

spring:
  application:
    name: customer-service

  r2dbc:
    url: r2dbc:postgresql://postgres:5432/customer_services
    username: tcam
    password: tcam123

  liquibase:
    enabled: true
    url: jdbc:postgresql://postgres:5432/customer_service
    user: tcam
    password: tcam123
    change-log: classpath:/db/changelog/master.xml

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"
